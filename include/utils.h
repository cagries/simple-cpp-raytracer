
/*
 * #ifndef UTILS_H
#define UTILS_H

#include "geometry.h"

namespace utils {
Vector cross(Vector a, Vector b) {
    return {
        a.y * b.z - a.z * b.y,
        a.z * b.x - a.x * b.z,
        a.x * b.y - a.y * b.x
    };
}

} // namespace utils

#endif UTILS_H
*/
